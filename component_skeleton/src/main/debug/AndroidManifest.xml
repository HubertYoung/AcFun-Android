<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kento.component_skeleton">

    <application
        android:name="com.kento.common.CommonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/skeleton_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="activity.RecyclerViewActivity" />
        <activity
            android:name="activity.ViewActivity"
            android:hardwareAccelerated="false" />
        <activity android:name="activity.StatusViewActivity"/>
    </application>

</manifest>